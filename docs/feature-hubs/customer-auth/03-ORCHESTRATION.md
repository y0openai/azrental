# 03-ORCHESTRATION.md - 고객 인증 (Customer Auth) Task 분배

> **Agent Task Distribution Plan**
> **작성일**: 2026-01-12
> **작성자**: Orchestrator Agent
> **모드**: Wave Orchestration (🟡 Moderate)

---

## 1. 복잡도 분석

### 1.1 복잡도 점수 계산
```
영향 모듈 수: 2 × 0.3 = 0.6   (auth, profile)
예상 일수: 2 × 0.2 = 0.4      (2일)
신규 API 수: 3 × 0.25 = 0.75  (signup, login, verification)
UI 화면 수: 4 × 0.15 = 0.6    (login, signup, forgot, reset)
외부 연동: 1 × 0.1 = 0.1      (Firebase Auth)
─────────────────────────
총 복잡도 점수: 2.45
```

### 1.2 복잡도 등급
| 복잡도 점수 | 등급 | Phase 수 | 이 기능 |
|------------|------|----------|--------|
| 1.0-2.0 | 🟢 Simple | 2-3 | |
| 2.1-3.5 | 🟡 Moderate | 3-4 | ✅ |
| 3.6-5.0 | 🟠 Complex | 4-5 | |
| 5.1+ | 🔴 Large | 5+ | |

### 1.3 에이전트 할당
**결정**: 3명 에이전트 투입 (🟡 Moderate 기준)

| Agent | 역할 | 담당 영역 |
|-------|------|----------|
| Agent 1 | Backend | Firebase Auth, Firestore, Server Actions |
| Agent 2 | Frontend Core | Hooks, Services, Validation |
| Agent 3 | Frontend UI | Components, Pages, Styling |

> **참고**: QA는 Phase 3에서 Orchestrator가 통합 테스트 수행

---

## 2. Wave 구조

```
Wave 0: Specification (완료)
├─ PRD 작성 ✅
├─ RFC 작성 ✅
└─ ORCHESTRATION 작성 ✅ (현재)

Wave 1: Foundation (병렬, 3명)
├─ Agent 1: Backend 인프라 구축
├─ Agent 2: Frontend Core 로직
└─ Agent 3: UI 컴포넌트

Wave 2: Integration (순차)
├─ Mock 제거 → 실제 연결
├─ 통합 테스트
└─ 버그 수정

Wave 3: Testing & Deploy (순차)
├─ E2E 테스트
├─ Staging 배포
└─ 검증

예상 총 소요: 8-10시간 (병렬로 단축)
```

---

## 3. Wave 1: Foundation (병렬)

### 3.1 Agent 1: Backend

**담당 영역**: Firebase Auth 설정, Firestore 스키마, Server Actions

#### Task 1.1: Firebase 설정 확인
- **설명**: Firebase Auth 및 Firestore 설정 확인/구성
- **산출물**: `src/lib/firebase.ts` (Admin SDK 포함)
- **예상 시간**: 30분
- **의존성**: 없음

#### Task 1.2: Firestore 스키마 구현
- **설명**: customers, email_verifications 컬렉션 타입 정의
- **산출물**:
  - `src/types/customer.ts`
  - `firestore.rules` 업데이트
- **예상 시간**: 45분
- **의존성**: Task 1.1

#### Task 1.3: Server Actions 구현
- **설명**: 인증 관련 Server Actions 구현
- **산출물**: `src/app/actions/auth.ts`
  - `sendVerificationCode`
  - `verifyCode`
  - `signUpWithEmail`
  - `signInWithEmail`
  - `sendPasswordResetEmail`
  - `signOut`
- **예상 시간**: 2시간
- **의존성**: Task 1.2

#### Task 1.4: Policy Config 설정
- **설명**: 인증 정책 설정 (Firestore Console)
- **산출물**: `policy_configs/auth` 문서
- **예상 시간**: 15분
- **의존성**: Task 1.2

---

### 3.2 Agent 2: Frontend Core

**담당 영역**: Hooks, Services, Validation, State Management

#### Task 2.1: Validation 스키마 정의
- **설명**: Zod를 사용한 폼 검증 스키마
- **산출물**: `src/lib/validations/auth.ts`
- **예상 시간**: 30분
- **의존성**: 없음

#### Task 2.2: Auth Service 구현
- **설명**: Server Actions 래퍼 + 에러 핸들링
- **산출물**: `src/services/authService.ts`
- **예상 시간**: 45분
- **의존성**: Task 2.1

#### Task 2.3: useAuth Hook 구현
- **설명**: 인증 상태 관리 Hook
- **산출물**: `src/hooks/useAuth.ts`
- **예상 시간**: 1시간
- **의존성**: Task 2.2

#### Task 2.4: Auth Context/Provider 구현
- **설명**: 전역 인증 상태 관리
- **산출물**: `src/contexts/AuthContext.tsx`
- **예상 시간**: 45분
- **의존성**: Task 2.3

---

### 3.3 Agent 3: Frontend UI

**담당 영역**: Components, Pages, Styling

#### Task 3.1: 공통 UI 컴포넌트 구현
- **설명**: Input, Button, Checkbox 등 기본 컴포넌트
- **산출물**:
  - `src/components/ui/Input.tsx`
  - `src/components/ui/Button.tsx`
  - `src/components/ui/Checkbox.tsx`
- **예상 시간**: 1시간
- **의존성**: 없음

#### Task 3.2: Auth 전용 컴포넌트 구현
- **설명**: 인증 관련 컴포넌트
- **산출물**:
  - `src/components/auth/LoginForm.tsx`
  - `src/components/auth/SignupForm.tsx`
  - `src/components/auth/EmailVerification.tsx`
  - `src/components/auth/PasswordStrengthIndicator.tsx`
  - `src/components/auth/AgreementCheckboxes.tsx`
  - `src/components/auth/SocialLoginButtons.tsx`
- **예상 시간**: 2시간
- **의존성**: Task 3.1

#### Task 3.3: Auth Pages 구현
- **설명**: 인증 페이지 레이아웃 및 라우팅
- **산출물**:
  - `src/app/(auth)/login/page.tsx`
  - `src/app/(auth)/signup/page.tsx`
  - `src/app/(auth)/forgot-password/page.tsx`
  - `src/app/(auth)/reset-password/page.tsx`
  - `src/app/(auth)/layout.tsx`
- **예상 시간**: 1.5시간
- **의존성**: Task 3.2

#### Task 3.4: 반응형 스타일링
- **설명**: Mobile-first 반응형 디자인
- **산출물**: Tailwind CSS (inline + globals.css)
- **예상 시간**: 45분
- **의존성**: Task 3.2, 3.3

---

## 4. Wave 2: Integration (순차)

### 4.1 Mock 제거 및 실제 연결
- **담당**: Orchestrator
- **설명**: Agent 1, 2, 3 결과물 통합
- **작업**:
  - Mock 데이터 제거
  - Server Actions ↔ Frontend 연결
  - 에러 핸들링 확인
- **예상 시간**: 1시간

### 4.2 통합 테스트
- **담당**: Orchestrator
- **설명**: Firebase Emulators 환경에서 테스트
- **작업**:
  - 회원가입 플로우 테스트
  - 로그인/로그아웃 테스트
  - 비밀번호 재설정 테스트
  - 에러 케이스 테스트
- **예상 시간**: 1시간

---

## 5. Wave 3: Testing & Deploy (순차)

### 5.1 E2E 테스트
- **담당**: Orchestrator
- **설명**: Playwright E2E 테스트
- **산출물**: `tests/e2e/auth.e2e.test.ts`
- **예상 시간**: 1시간

### 5.2 Staging 배포
- **담당**: Orchestrator
- **설명**: Vercel Preview 배포 및 검증
- **작업**:
  - `npm run build` 성공 확인
  - Vercel Preview 배포
  - 실제 환경 테스트
- **예상 시간**: 30분

### 5.3 최종 검증 및 문서화
- **담당**: Orchestrator
- **설명**: 최종 점검 및 .handoff.md 업데이트
- **작업**:
  - 체크리스트 완료 확인
  - 문서 업데이트
  - PR 생성 (필요 시)
- **예상 시간**: 30분

---

## 6. 의존성 다이어그램

```
Wave 1 (병렬 작업):
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  Agent 1 (Backend)     Agent 2 (Core)      Agent 3 (UI)        │
│  ┌───────────────┐    ┌───────────────┐   ┌───────────────┐    │
│  │ 1.1 Firebase  │    │ 2.1 Validation│   │ 3.1 UI Base   │    │
│  │      ↓        │    │      ↓        │   │      ↓        │    │
│  │ 1.2 Schema    │    │ 2.2 Service   │   │ 3.2 Auth Comp │    │
│  │      ↓        │    │      ↓        │   │      ↓        │    │
│  │ 1.3 Actions   │    │ 2.3 Hook      │   │ 3.3 Pages     │    │
│  │      ↓        │    │      ↓        │   │      ↓        │    │
│  │ 1.4 Policy    │    │ 2.4 Context   │   │ 3.4 Styling   │    │
│  └───────────────┘    └───────────────┘   └───────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
Wave 2 (통합):
┌─────────────────────────────────────────────────────────────────┐
│  Orchestrator                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Mock 제거 → 실제 연결 → 통합 테스트                       │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
Wave 3 (배포):
┌─────────────────────────────────────────────────────────────────┐
│  Orchestrator                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ E2E 테스트 → Staging 배포 → 최종 검증                     │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. Mock 인터페이스 정의

### 7.1 Backend Mock (Agent 2가 사용)
```typescript
// Agent 2가 Agent 1 완료 전 사용
export const mockAuthActions = {
  sendVerificationCode: async (email: string) => ({
    success: true,
    message: '인증코드가 발송되었습니다'
  }),

  verifyCode: async (email: string, code: string) => ({
    success: code === '123456',
    error: code !== '123456' ? '잘못된 인증코드입니다' : undefined
  }),

  signUpWithEmail: async (data: SignUpData) => ({
    success: true,
    user: {
      uid: 'mock-uid',
      email: data.email,
      displayName: data.displayName
    }
  }),

  signInWithEmail: async (email: string, password: string) => ({
    success: password === 'Test1234',
    user: password === 'Test1234' ? {
      uid: 'mock-uid',
      email,
      displayName: '테스트 사용자'
    } : undefined,
    error: password !== 'Test1234' ? '비밀번호가 일치하지 않습니다' : undefined
  })
};
```

### 7.2 Hook Mock (Agent 3가 사용)
```typescript
// Agent 3가 Agent 2 완료 전 사용
export const mockUseAuth = () => ({
  user: null,
  loading: false,
  error: null,

  sendVerificationCode: jest.fn().mockResolvedValue({ success: true }),
  verifyCode: jest.fn().mockResolvedValue({ success: true }),
  signUp: jest.fn().mockResolvedValue({ success: true }),
  signIn: jest.fn().mockResolvedValue({ success: true }),
  signOut: jest.fn().mockResolvedValue(undefined),
  sendPasswordReset: jest.fn().mockResolvedValue({ success: true })
});
```

---

## 8. 완료 기준 (Definition of Done)

### 8.1 Wave 1 완료 조건
- [ ] Agent 1: Server Actions 모두 구현 및 타입 안전
- [ ] Agent 2: useAuth Hook 동작 확인 (Mock 환경)
- [ ] Agent 3: 모든 페이지 UI 렌더링 확인

### 8.2 Wave 2 완료 조건
- [ ] Mock 제거 완료
- [ ] Firebase Emulators에서 회원가입/로그인 동작
- [ ] 에러 핸들링 정상 동작

### 8.3 Wave 3 완료 조건
- [ ] `npm run build` 성공
- [ ] E2E 테스트 통과
- [ ] Staging 환경 검증 완료

---

## 9. 일정 요약

| Wave | Agent | Task | 예상 시간 |
|------|-------|------|----------|
| 1 | Agent 1 | Task 1.1-1.4 | 3.5시간 |
| 1 | Agent 2 | Task 2.1-2.4 | 3시간 |
| 1 | Agent 3 | Task 3.1-3.4 | 5.25시간 |
| 2 | Orchestrator | 통합 | 2시간 |
| 3 | Orchestrator | 테스트/배포 | 2시간 |

**Wave 1 (병렬)**: ~5.25시간 (가장 긴 Agent 기준)
**Wave 2+3 (순차)**: ~4시간
**총 예상 시간**: ~9.25시간

---

## 10. 리스크 및 대응

| 리스크 | 영향 | 확률 | 대응 |
|--------|------|------|------|
| Firebase 설정 오류 | High | Low | 공식 문서 참조, Emulator 테스트 |
| 소셜 로그인 지연 | Medium | Medium | Phase 1.5로 연기 (PRD 허용) |
| 타입 불일치 | Medium | Medium | Mock 인터페이스 사전 정의 |
| 스타일 불일치 | Low | Low | 와이어프레임 참조 |

---

## 11. PM 승인 요청

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   📋 Customer Auth Wave Orchestration Plan                      │
│                                                                 │
│   복잡도: 2.45 (🟡 Moderate)                                    │
│   에이전트: 3명                                                 │
│   Wave 수: 3개 (Foundation → Integration → Deploy)             │
│   예상 시간: 9.25시간                                           │
│                                                                 │
│   ⚠️ 참고: 소셜 로그인(카카오/네이버)은 Phase 1.5로 연기       │
│                                                                 │
│   승인 후 Wave 1 병렬 실행을 시작합니다.                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

승인하시겠습니까? [Y/N]
```

---

*문서 버전: 1.0*
*최종 수정: 2026-01-12*
